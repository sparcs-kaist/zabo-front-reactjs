import React, { PureComponent } from "react"
import PropTypes from "prop-types"

import MyButtonsWrapper from "./MyButtons.styled"

class MyButtons extends PureComponent {
	render() {
		return (
			<MyButtonsWrapper>
			</MyButtonsWrapper>
		)
	}
}

MyButtons.propTypes = {
}

MyButtons.defaultProps = {
}

export default MyButtons
